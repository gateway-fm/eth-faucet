import base from './base/index.js';
import teiza from './teiza/index.js';

// Brand registry. Add a client = add a directory here + one entry.
export const brands = {
  base,
  teiza,
};

export const DEFAULT_BRAND = 'base';
